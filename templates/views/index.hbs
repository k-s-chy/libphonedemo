<!DOCTYPE html>

<html>

<head>
    <title>{{{title}}}</title>
    <link rel="stylesheet" href='/css/styles.css'>
</head>

<body>
    <div class="main-content">
        {{>header}}

        <form action = '/' onsubmit="return validateForm()" method = 'POST' align = 'center' autocomplete="off">
            <div><input name="numberInput" type="text" placeholder="Enter First Number" id="firstInput" oninput="this.value = this.value.replace(/[^0-9.+-]/g, '').replace(/(\..*)\./g, '$1');" required></div>
            <div><input name="numberInput" placeholder="Enter Second Number" id="secondInput" oninput="this.value = this.value.replace(/[^0-9.+-]/g, '').replace(/(\..*)\./g, '$1');"></div>
            <div><input name="numberInput" placeholder="Enter Third Number" id="thirdInput" oninput="this.value = this.value.replace(/[^0-9.+-]/g, '').replace(/(\..*)\./g, '$1');"></div>
        
            <button>Search</button>
        </form>

        <p id="message-1"></p>
        <p id="message-2"></p>

    </div>
    <script src='./js/app.js'></script>

</body>

</html>